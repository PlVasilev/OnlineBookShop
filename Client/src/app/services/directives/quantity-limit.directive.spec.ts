import { QuantityLimitDirective } from './quantity-limit.directive';

describe('QuantityLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityLimitDirective();
    expect(directive).toBeTruthy();
  });
});
